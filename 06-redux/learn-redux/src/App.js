import React from "react";
import CounterContainer from "./components/CounterContainer";

function App({ store }) {
  return (
    <div>
      <CounterContainer />
    </div>
  );
}

export default App;
